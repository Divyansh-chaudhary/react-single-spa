import '@testing-library/jest-dom'; // For extended matchers like `toBeInTheDocument`

describe('App Component', () => {
  test('renders the App component with the correct appname prop', () => {
    expect(1).toEqual(1);
  });
});
